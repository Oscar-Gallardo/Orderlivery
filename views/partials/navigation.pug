nav.navigation
  .navigation__wrapper
    a.navigation__link(href="/")
      img.navigation__link__icon(src=navigation.data.logo.url alt=navigation.data.logo.alt)

    ul.navigation__list
      each item in navigation.data.list
        li.navigation__list__item
          a.navigation__list__link(href=Link(item.link))=item.text
